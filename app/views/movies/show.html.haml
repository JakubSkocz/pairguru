%h1= @movie.title
.jumbotron
  = @movie.description
- if user_signed_in?  
  %div.container
    %br
    = render 'comments/shared/form'   
    %br
    = render 'comments/shared/comments_table', comments: @comments

  %p= link_to 'Email me details about this movie', send_info_movie_path(@movie), class: 'btn btn-sm btn-default'


